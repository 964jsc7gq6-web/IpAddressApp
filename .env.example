# App Ipê - Configuração de Ambiente
# Copie este arquivo para .env e preencha com os valores reais

# ======================
# BANCO DE DADOS
# ======================
# URL de conexão com PostgreSQL (obrigatório)
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://postgres:senha@localhost:5432/app_ipe

# ======================
# SEGURANÇA
# ======================
# Segredo para sessões (obrigatório em produção, mínimo 32 caracteres)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=sua-chave-secreta-aqui-minimo-32-caracteres

# Segredo para tokens JWT (OBRIGATÓRIO em produção, mínimo 32 caracteres)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=sua-chave-jwt-secreta-aqui-minimo-32-caracteres

# ======================
# SERVIDOR
# ======================
# Ambiente de execução (development | production)
NODE_ENV=development

# Porta do servidor (padrão: 5000)
PORT=5000

# Host de bind (padrão: 0.0.0.0)
HOST=0.0.0.0

# ======================
# ARMAZENAMENTO
# ======================
# Diretório para armazenar arquivos enviados (padrão: uploads)
# Use caminho relativo à raiz do projeto
UPLOADS_DIR=uploads
